# v2-구현
날짜: 2026-05-16

## 세션 개요
v2 기능(과일 아이콘, 점수 입력, 결과 화면) 구현 완료.

## 작업 로그

### FruitHelper.kt 생성
- 행동: `FRUIT_EMOJIS` 상수(8개 과일) + `FruitHelper` object 작성
- 결정: SharedPreferences 키 접두어 `fruit_p` + 1-indexed (fruit_p1, fruit_p2...)
- 기본값: P1=🍎(0), P2=🍌(1), P3=🍇(2), P4=🍓(3)

### strings.xml 추가
- 행동: v2 문자열 18개 추가 (과일 아이콘, 점수 입력, 결과 화면 관련)

### ScoreInputScreen.kt 생성
- 행동: 플레이어별 OutlinedTextField (KeyboardType.Number) 입력 화면 작성
- 결정: 점수 입력은 선택 사항, 빈 칸은 null로 처리
- 결정: `mutableStateListOf`로 각 플레이어 입력값 관리

### ResultScreen.kt 생성
- 행동: 랭킹 테이블 (점수 오름차순, null 점수는 후순위) 작성
- 결정: `findActivity()`를 ResultScreen에서도 사용 → HomeScreen.kt에서 `private` 제거
- 결정: 완료 버튼에서 광고 표시 후 `onComplete()` 호출 (게임 초기화)

### MainActivity.kt 리팩터
- 행동: `showSettings: Boolean` → `Screen` enum (HOME, SETTINGS, SCORE_INPUT, RESULT)
- 행동: `playerCount`, `penalties`, `scores`, `fruitIndices` 상태를 RummitimerApp으로 끌어올림
- 결정: `penalties`, `scores`, `fruitIndices`는 `remember` 사용 (구성 변경 시 초기화 허용)
- 결정: `currentScreen`, `playerCount`는 `rememberSaveable` 사용

### HomeScreen.kt 수정
- 행동: `playerCount` 로컬 상태 제거 → 파라미터로 수신
- 행동: `adManager` 제거 → End 버튼은 광고 없이 `onEndGame()` 호출
- 행동: `rememberUpdatedState(onPenalty)` → 타이머 종료 시 패널티 +1
- 행동: `PlayerTurnLabel`, `SplitTimerCircle`에 `fruitEmoji` 파라미터 추가
- 행동: `PlayerIndicators`에 `fruitIndices` 추가 → 원 아래 과일 이모지 표시
- 행동: `findActivity()`에서 `private` 제거 → ResultScreen에서도 사용 가능

### SettingsScreen.kt 수정
- 행동: `fruitIndices`, `onFruitChange` 파라미터 추가
- 행동: `FruitPickerRow`, `FruitChip` 컴포저블 추가 (가로 스크롤 지원)
- 결정: 40dp 칩, `horizontalScroll`로 화면 넘침 처리

## 결과
- BUILD SUCCESSFUL (9s)
- 신규 파일: FruitHelper.kt, ScoreInputScreen.kt, ResultScreen.kt
- 수정 파일: MainActivity.kt, HomeScreen.kt, SettingsScreen.kt, strings.xml
